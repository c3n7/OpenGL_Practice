jobs:
    include:
        - os: linux
          dist: bionic
        - os: osx

language: cpp

addons:
    apt:
        packages:
            - xorg-dev

before_install:
    - git submodule init
    - git submodule update
script:
    - cmake .
    - cmake --build .
